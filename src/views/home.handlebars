{{!-- Layout principal --}}
{{#extend 'layouts/main'}}
{{#content 'body'}}
    <h1>Lista de Productos</h1>
    <ul>
        {{#each products}}
            <li>{{this.title}} - ${{this.price}}</li>
        {{/each}}
    </ul>
{{/content}}
{{/extend}}
